Conventions Vue 3 + @vue/test-utils spécifiques à ce dossier. Global (commandes, architecture, flux données) → `CLAUDE.md` racine.

## Tests

Vitest, `globals:true`, env jsdom, setup `src/test-setup.ts`.
